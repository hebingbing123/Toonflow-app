/// Future: register atomic tools for the agent loop.
#[derive(Default)]
#[allow(dead_code)]
pub struct ToolRegistry;

#[allow(dead_code)]
impl ToolRegistry {
    pub fn new() -> Self {
        Self
    }
}
