# Local development — Rust + Flutter + Supabase CLI（本机若已有其它 Supabase 占用 5432x，见仓库 supabase/config.toml 中的 643xx 端口）
#
# Flutter：`cd frontend && flutter run -d chrome --dart-define-from-file=dart_defines.dev.json`（与下方 URL/anon key 保持一致；改端口时请同步改 frontend/dart_defines.dev.json）
TOONFLOW_ENV=dev

# Rust API（backend 默认；也可写在 backend/.env）
PORT=8666
API_BASE_URL=http://127.0.0.1:8666

# Supabase（`supabase status -o env` 可复制；本地 demo 密钥与官方 CLI 一致）
SUPABASE_URL=http://127.0.0.1:64321
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:64322/postgres
SUPABASE_JWT_SECRET=super-secret-jwt-token-with-at-least-32-characters-long
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0

# 若 `supabase start` 因 storage 健康检查失败：可试 `supabase start --ignore-health-check`（仅本地；Postgres/Auth 仍可用）。
# 若 `supabase db reset` 在「迁移/seed 已完成」后仍因 storage 未就绪退出 1（Colima 常见）：跑 `./scripts/supabase_db_reset_local.sh`，或手动 `supabase start --ignore-health-check`。
