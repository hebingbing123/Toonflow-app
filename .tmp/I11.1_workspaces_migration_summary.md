# Task 11.1: Workspaces Module I18n Migration Summary

## Overview
Successfully migrated all error call sites in the `backend/src/workspaces/` module to use bilingual helper functions.

## Files Modified
1. `backend/src/workspaces/http.rs`
2. `backend/src/workspaces/ops_stats.rs`

## Migration Statistics

### Conflict Errors (9 instances migrated)
All `ApiError::Conflict` constructors replaced with `conflict_i18n()` helper:

1. **Line ~1123**: "cannot demote the last workspace owner" → "不能降级最后一个工作区所有者"
2. **Line ~1197**: "cannot remove the last workspace owner" → "不能移除最后一个工作区所有者"
3. **Line ~1275**: "cannot leave workspace as the last owner" → "不能作为最后一个所有者离开工作区"
4. **Line ~1339**: "target owner must differ from current owner" → "目标所有者必须与当前所有者不同"
5. **Line ~1366**: "only the current primary owner may transfer ownership" → "只有当前主要所有者可以转移所有权"
6. **Line ~1380**: "target user must already be a workspace member" → "目标用户必须已经是工作区成员"
7. **Line ~1687**: "invite cannot be revoked (not pending)" → "邀请无法撤销（不是待处理状态）"
8. **Line ~1797**: "invite cannot be resent (not pending)" → "邀请无法重发（不是待处理状态）"
9. **Line ~1998**: "invite is not pending" → "邀请不是待处理状态"
10. **Line ~2001**: "invite has expired" → "邀请已过期"

### Forbidden Errors (4 instances migrated)
All `ApiError::Forbidden` constructors replaced with `forbidden_i18n()` helper:

1. **ops_stats.rs Line ~39**: "workspace stats HTTP disabled (set TOONFLOW_INTERNAL_OPS_TOKEN)" → "工作区统计 HTTP 已禁用（请设置 TOONFLOW_INTERNAL_OPS_TOKEN）"
2. **http.rs Line ~283**: "not a workspace member" → "不是工作区成员"
3. **http.rs Line ~295**: "requires workspace owner or admin" → "需要工作区所有者或管理员权限"
4. **http.rs Line ~310**: "requires workspace owner" → "需要工作区所有者权限"

### BadRequest Errors
No `ApiError::BadRequest` instances found - already using `bad_request_i18n()` helper.

### NotImplemented Errors
No `ApiError::NotImplemented` instances found in this module.

## Import Changes

### backend/src/workspaces/http.rs
```rust
// Before:
use crate::error::{bad_request_i18n, ApiError};

// After:
use crate::error::{bad_request_i18n, conflict_i18n, forbidden_i18n, ApiError};
```

### backend/src/workspaces/ops_stats.rs
```rust
// Before:
use crate::error::ApiError;

// After:
use crate::error::{forbidden_i18n, ApiError};
```

## Migration Pattern Examples

### Conflict Error Migration
```rust
// Before:
return Err(ApiError::Conflict(
    "cannot demote the last workspace owner".into(),
));

// After:
return Err(conflict_i18n(
    "cannot demote the last workspace owner",
    "不能降级最后一个工作区所有者",
));
```

### Forbidden Error Migration
```rust
// Before:
Err(ApiError::Forbidden("requires workspace owner".into()))

// After:
Err(forbidden_i18n(
    "requires workspace owner",
    "需要工作区所有者权限",
))
```

## Verification

### Compilation Check
✅ `cargo check` passed successfully

### Test Results
✅ All 15 workspaces module tests passed
- 0 failed
- 1 ignored (requires database setup)
- Test suite: `cargo test --lib workspaces`

### Pattern Verification
✅ No remaining `ApiError::Conflict(` patterns in workspaces module
✅ No remaining `ApiError::Forbidden(` patterns in workspaces module

## Language Selection Behavior

All migrated errors now support automatic language selection based on the `Accept-Language` request header:
- `Accept-Language: zh` or `zh-CN` → Chinese error messages
- `Accept-Language: en` or missing → English error messages (default)

## Next Steps

According to the task plan, the next task is:
- **Task 11.2**: Add/update tests for workspaces module to verify both English and Chinese error messages
- **Task 11.3**: Update migration progress tracker in MIGRATION_GUIDE.md

## Notes

- All error messages maintain the same semantic meaning in both languages
- The migration preserves backward compatibility - existing API clients will continue to receive error responses in the same JSON structure
- Error response structure remains unchanged: `{ status, code, message, request_id, details, retry_after_ms }`
- The bilingual helpers (`conflict_i18n`, `forbidden_i18n`) automatically select the appropriate language based on `REQUEST_LOCALE` task-local variable
