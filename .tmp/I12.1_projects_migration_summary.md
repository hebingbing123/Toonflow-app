# Task 12.1: Projects Module I18n Migration Summary

## Overview
Successfully migrated all error call sites in the `backend/src/projects/` module to use bilingual helper functions.

## Files Modified
1. `backend/src/projects/routes/common.rs`
2. `backend/src/projects/routes/export.rs`
3. `backend/src/projects/routes/handlers/create_list/create.rs`
4. `backend/src/projects/routes/handlers/detail/style_config.rs`
5. `backend/src/projects/routes/handlers/detail/patch.rs`
6. `backend/src/projects/routes/handlers/detail/overview.rs`
7. `backend/src/projects/routes/handlers/detail/home.rs`
8. `backend/src/projects/routes/handlers/detail/members.rs`
9. `backend/src/projects/routes/handlers/detail/delete.rs`
10. `backend/src/projects/routes/tts.rs`

## Migration Statistics

### BadRequest Errors (15 instances migrated)
All `ApiError::BadRequest` constructors replaced with `bad_request_i18n()` helper:

1. **create.rs Line ~67**: "cannot create project in archived workspace" → "无法在已归档的工作区中创建项目"
2. **create.rs Line ~186**: "invalid projectBrief: {e}" → "projectBrief 无效：{e}"
3. **create.rs Line ~193**: "invalid brandBible: {e}" → "brandBible 无效：{e}"
4. **style_config.rs Line ~59**: "expected at least one field: artStylePack or storyStylePack" → "至少需要一个字段：artStylePack 或 storyStylePack"
5. **patch.rs Line ~74**: "{field} must be a valid object: {e}" → "{field} 必须是有效的对象：{e}"
6. **patch.rs Line ~78**: "{field} must be an object or null" → "{field} 必须是对象或 null"
7. **patch.rs Line ~200**: "target_platforms must be array or null" → "target_platforms 必须是数组或 null"
8. **patch.rs Line ~245**: "expected at least one patchable field (...)" → "至少需要一个可修改的字段 (...)"
9. **overview.rs Line ~177**: "Failed to serialize response: {}" → "序列化响应失败：{}"
10. **overview.rs Line ~184**: "Failed to deserialize response: {}" → "反序列化响应失败：{}"
11. **home.rs Line ~280**: "invalid project_brief in storage: {e}" → "存储中的 project_brief 无效：{e}"
12. **home.rs Line ~285**: "invalid brand_bible in storage: {e}" → "存储中的 brand_bible 无效：{e}"
13. **members.rs Line ~50**: "target user must already be a workspace member" → "目标用户必须已经是工作区成员"
14. **members.rs Line ~56**: "project owner already has full access" → "项目所有者已拥有完全访问权限"
15. **members.rs Line ~62**: "workspace owner/admin already has full access" → "工作区所有者/管理员已拥有完全访问权限"
16. **tts.rs Line ~201**: "text is empty and shot has no prompt fallback" → "文本为空且镜头没有提示词后备"

### Conflict Errors (1 instance migrated)
All `ApiError::Conflict` constructors replaced with `conflict_i18n()` helper:

1. **export.rs Line ~329**: "export task cannot be cancelled in current status" → "导出任务在当前状态下无法取消"

### Forbidden Errors (9 instances migrated)
All `ApiError::Forbidden` constructors replaced with `forbidden_i18n()` helper:

1. **common.rs Line ~162**: "project has been archived by platform operators" → "项目已被平台运营方归档"
2. **common.rs Line ~199**: "not a member of project workspace" → "不是项目工作区成员"
3. **common.rs Line ~217**: "project requires explicit viewer or editor access" → "项目需要明确的查看者或编辑者访问权限"
4. **common.rs Line ~253**: "project requires explicit editor access for mutations" → "项目需要明确的编辑者访问权限才能进行修改"
5. **common.rs Line ~268**: "requires workspace owner/admin or project owner" → "需要工作区所有者/管理员或项目所有者权限"
6. **export.rs Line ~117**: "project requires explicit editor access for export" → "项目需要明确的编辑者访问权限才能导出"
7. **export.rs Line ~303**: "project requires explicit editor access for export cancellation" → "项目需要明确的编辑者访问权限才能取消导出"
8. **create.rs Line ~60**: "not a member of the target workspace" → "不是目标工作区的成员"
9. **delete.rs Line ~44**: "only the project owner or workspace owner/admin can delete this project" → "只有项目所有者或工作区所有者/管理员可以删除此项目"

### NotImplemented Errors
No `ApiError::NotImplemented` instances found in this module.

## Import Changes

### backend/src/projects/routes/common.rs
```rust
// Before:
use crate::{error::ApiError, state::AppState};

// After:
use crate::{
    error::{forbidden_i18n, ApiError},
    state::AppState,
};
```

### backend/src/projects/routes/export.rs
```rust
// Before:
use crate::{
    auth::require_user_uuid,
    error::ApiError,
    projects::{models::export_task::*, routes::common::require_project_workspace_member_scope},
    state::AppState,
};

// After:
use crate::{
    auth::require_user_uuid,
    error::{conflict_i18n, forbidden_i18n, ApiError},
    projects::{models::export_task::*, routes::common::require_project_workspace_member_scope},
    state::AppState,
};
```

### backend/src/projects/routes/handlers/create_list/create.rs
```rust
// Before:
use crate::error::ApiError;

// After:
use crate::error::{bad_request_i18n, forbidden_i18n, ApiError};
```

### backend/src/projects/routes/handlers/detail/style_config.rs
```rust
// Before:
use crate::error::ApiError;

// After:
use crate::error::{bad_request_i18n, ApiError};
```

### backend/src/projects/routes/handlers/detail/patch.rs
```rust
// Before:
use crate::error::ApiError;

// After:
use crate::error::{bad_request_i18n, ApiError};
```

### backend/src/projects/routes/handlers/detail/overview.rs
```rust
// Before:
use crate::{
    auth::require_user_uuid,
    error::ApiError,
    ...
};

// After:
use crate::{
    auth::require_user_uuid,
    error::{bad_request_i18n, ApiError},
    ...
};
```

### backend/src/projects/routes/handlers/detail/home.rs
```rust
// Before:
use crate::error::ApiError;

// After:
use crate::error::{bad_request_i18n, ApiError};
```

### backend/src/projects/routes/handlers/detail/delete.rs
```rust
// Before:
use crate::error::ApiError;

// After:
use crate::error::{forbidden_i18n, ApiError};
```

## Migration Pattern Examples

### BadRequest Error Migration
```rust
// Before:
return Err(ApiError::BadRequest(
    "cannot create project in archived workspace".into(),
));

// After:
return Err(bad_request_i18n(
    "cannot create project in archived workspace",
    "无法在已归档的工作区中创建项目",
));
```

### Conflict Error Migration
```rust
// Before:
return Err(ApiError::Conflict(
    "export task cannot be cancelled in current status".into(),
));

// After:
return Err(conflict_i18n(
    "export task cannot be cancelled in current status",
    "导出任务在当前状态下无法取消",
));
```

### Forbidden Error Migration
```rust
// Before:
Err(ApiError::Forbidden(
    "project requires explicit editor access for mutations".into(),
))

// After:
Err(forbidden_i18n(
    "project requires explicit editor access for mutations",
    "项目需要明确的编辑者访问权限才能进行修改",
))
```

### Dynamic Message Migration
```rust
// Before:
.map_err(|e| ApiError::BadRequest(format!("invalid projectBrief: {e}")))

// After:
.map_err(|e| bad_request_i18n(
    &format!("invalid projectBrief: {e}"),
    &format!("projectBrief 无效：{e}")
))
```

## Verification

### Compilation Check
✅ `cargo fmt` completed successfully
✅ `cargo clippy` passed with no warnings
✅ `yarn refactor:agent` passed all checks

### Pattern Verification
✅ No remaining `ApiError::BadRequest(` patterns in projects module (except test files)
✅ No remaining `ApiError::Conflict(` patterns in projects module
✅ No remaining `ApiError::Forbidden(` patterns in projects module

## Language Selection Behavior

All migrated errors now support automatic language selection based on the `Accept-Language` request header:
- `Accept-Language: zh` or `zh-CN` → Chinese error messages
- `Accept-Language: en` or missing → English error messages (default)

## Next Steps

According to the task plan, the next tasks are:
- **Task 12.2**: Add/update tests for projects module to verify both English and Chinese error messages
- **Task 12.3**: Update migration progress tracker in MIGRATION_GUIDE.md

## Notes

- All error messages maintain the same semantic meaning in both languages
- The migration preserves backward compatibility - existing API clients will continue to receive error responses in the same JSON structure
- Error response structure remains unchanged: `{ status, code, message, request_id, details, retry_after_ms }`
- The bilingual helpers (`bad_request_i18n`, `conflict_i18n`, `forbidden_i18n`) automatically select the appropriate language based on `REQUEST_LOCALE` task-local variable
- Dynamic error messages with variable interpolation use `format!()` macro with both English and Chinese templates
- All code has been formatted with `cargo fmt` to comply with Rust style guidelines

## Total Migration Count

- **BadRequest errors**: 16 instances
- **Conflict errors**: 1 instance
- **Forbidden errors**: 9 instances
- **NotImplemented errors**: 0 instances
- **Total**: 26 error call sites migrated across 10 files
