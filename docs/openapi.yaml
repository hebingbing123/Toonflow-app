openapi: 3.1.0
info:
  title: Toonflow API
  version: 1.0.0
  description: |
    HTTP surface for the Rust backend (`backend/`). All versioned routes live under `/api/v1`.
    WebSocket: `GET /api/v1/ws` (Upgrade) — JSON envelope protocol in `docs/websocket-events.md`; optional query `access_token`.

servers:
  - url: http://127.0.0.1:8666
    description: Local dev (default `PORT`; override with env)

tags:
  - name: system
    description: Health and probes
  - name: session
    description: Auth probe (Supabase JWT)
  - name: projects
    description: User-owned projects and scripts (Postgres + RLS)

paths:
  /health:
    get:
      tags: [system]
      summary: Liveness (unversioned)
      operationId: healthRoot
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"

  /api/v1/health:
    get:
      tags: [system]
      summary: Liveness (versioned)
      operationId: healthV1
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"

  /api/v1/ready:
    get:
      tags: [system]
      summary: Readiness (optional database ping)
      operationId: readyV1
      description: |
        If `DATABASE_URL` is set, runs `SELECT 1`. Otherwise returns `database: not_configured` (HTTP 200).
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadyResponse"
        "503":
          description: Database unreachable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"

  /api/v1/me:
    get:
      tags: [session]
      summary: Current user from JWT (no DB round-trip)
      operationId: meV1
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeResponse"
        "401":
          description: Missing or invalid Bearer token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "503":
          description: `SUPABASE_JWT_SECRET` not configured on server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"

  /api/v1/projects:
    get:
      tags: [projects]
      summary: List projects for the current user
      operationId: listProjectsV1
      description: |
        Returns `app_project` rows where `owner_user_id` matches the JWT `sub`.
        Requires `DATABASE_URL` on the server (otherwise 503).
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProjectRow"
        "401":
          description: Missing or invalid Bearer token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "503":
          description: Auth not configured, database error, or `DATABASE_URL` unset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"

  /api/v1/projects/legacy/{legacy_id}:
    get:
      tags: [projects]
      summary: Project detail by legacy integer id
      operationId: getProjectByLegacyIdV1
      description: |
        Resolves `app_project` by `legacy_id` and current user; includes child `app_script` rows.
      security:
        - bearerAuth: []
      parameters:
        - name: legacy_id
          in: path
          required: true
          schema:
            type: integer
            format: int32
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectDetailResponse"
        "401":
          description: Missing or invalid Bearer token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "404":
          description: No project with that legacy id for this user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "503":
          description: Auth not configured, database error, or `DATABASE_URL` unset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
    patch:
      tags: [projects]
      summary: Partially update project by legacy id
      operationId: patchProjectByLegacyIdV1
      description: |
        JSON merge patch for `name` and `intro`. Omit a key to leave that column unchanged;
        `null` or empty string clears the column. At least one of `name` or `intro` must appear in the body.
      security:
        - bearerAuth: []
      parameters:
        - name: legacy_id
          in: path
          required: true
          schema:
            type: integer
            format: int32
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatchProjectBody"
      responses:
        "200":
          description: Updated project row
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectRow"
        "400":
          description: Invalid body or no fields to apply
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "401":
          description: Missing or invalid Bearer token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "404":
          description: No project with that legacy id for this user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"
        "503":
          description: Auth not configured, database error, or `DATABASE_URL` unset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorBody"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        Supabase-issued access token (`Authorization: Bearer <jwt>`).
        Rust validates signature/claims per Supabase project settings (see architecture plan §4.2).

  schemas:
    HealthResponse:
      type: object
      required: [status, service]
      properties:
        status:
          type: string
          example: ok
        service:
          type: string
          example: toonflow-server

    ReadyResponse:
      type: object
      required: [status, database]
      properties:
        status:
          type: string
          example: ok
        database:
          type: string
          enum: [not_configured, connected]
          description: `not_configured` when `DATABASE_URL` is unset

    MeResponse:
      type: object
      required: [sub]
      properties:
        sub:
          type: string
          format: uuid
          description: Supabase Auth user id (`JWT sub`)
        email:
          type: string
          format: email
          description: Present when included in JWT claims

    ProjectRow:
      type: object
      required: [id, legacy_id]
      properties:
        id:
          type: string
          format: uuid
        legacy_id:
          type: integer
          format: int32
        name:
          type: string
          nullable: true
        intro:
          type: string
          nullable: true
        project_type:
          type: string
          nullable: true
        create_time_ms:
          type: integer
          format: int64
          nullable: true

    ScriptBrief:
      type: object
      required: [legacy_id]
      properties:
        legacy_id:
          type: integer
          format: int32
        name:
          type: string
          nullable: true
        extract_state:
          type: integer
          format: int32
          nullable: true

    ProjectDetailResponse:
      type: object
      required: [project, scripts]
      properties:
        project:
          $ref: "#/components/schemas/ProjectRow"
        scripts:
          type: array
          items:
            $ref: "#/components/schemas/ScriptBrief"

    PatchProjectBody:
      type: object
      description: |
        At least one property required. Use `null` or `""` to clear `name` or `intro`.
      properties:
        name:
          type: string
          nullable: true
        intro:
          type: string
          nullable: true

    ErrorBody:
      type: object
      required: [code, message]
      description: |
        Stable error envelope for `/api/v1/*`. Clients should log `request_id` for support.
      properties:
        code:
          type: string
          description: Machine-readable error code (snake_case)
          example: unauthorized
        message:
          type: string
          description: Human-readable summary
        request_id:
          type: string
          format: uuid
          description: Correlates with server logs / tracing
        details:
          type: object
          additionalProperties: true
          description: Optional structured detail (validation fields, etc.)

    RateLimitError:
      allOf:
        - $ref: "#/components/schemas/ErrorBody"
        - type: object
          properties:
            code:
              const: rate_limited
            retry_after_ms:
              type: integer
              description: Suggested backoff in milliseconds (mirror `Retry-After` when present)
