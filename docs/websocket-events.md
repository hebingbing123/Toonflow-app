# WebSocket events (`/api/v1/ws`)

**Normative copy:** the full wire protocol (envelopes, `session.auth`, Harness events, legacy mapping, errors) lives **in `docs/openapi.yaml`** under **`paths` → `/api/v1/ws` → `get` → `description`**.

**For humans:** open the YAML and search for `/api/v1/ws:`, or use **Swagger UI** at `GET /api/v1/docs` and expand **GET /api/v1/ws**.

This file stays in the repo so links from `AGENTS.md`, parity docs, and PR templates keep a stable path; edit the OpenAPI block when the protocol changes, then refresh this one-line pointer if needed.
